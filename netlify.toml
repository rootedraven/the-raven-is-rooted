# Static site — no build step
[build]
  publish = "."

# Optional: discourage search engines on the Netlify subdomain (remove when you go public)
[[headers]]
  for = "/*"
  [headers.values]
    X-Robots-Tag = "noindex, nofollow"
